# Changelog

# 43
# 18-09-2026-2200-WIB
- **Hapus dead code yang tidak pernah direferensikan**: `util/DynamicColor.kt` (duplikat dari `overlay/DynamicColors.kt` yang asli), `overlay/PanelLifecycleOwner.kt` (tidak dipanggil dari mana pun), `res/drawable/ic_collapse.xml` (sudah diganti grab-handle di changelog #28 tapi file-nya masih ada), dan enum `OverlayOrientation` + field `orientation` di `OverlayConfig` (didefinisikan tapi tidak pernah di-read atau di-write oleh UI maupun logic). Model test yang mereferensikan `OverlayOrientation` ikut diupdate.
- **Wire up `OverlayWebView.applySettings()`** — sebelumnya method ini terdefinisi di `OverlayWebView.kt` tapi TIDAK PERNAH dipanggil dari mana pun, jadi toggle JavaScript/Media/Cache/Cookies di Settings tidak punya efek apa pun ke WebView overlay. Sekarang dipanggil dari `OverlayService.startOverlay()` setelah `window.create()`, baca nilai dari `AppSettings` (javaScriptEnabled, mediaPlaybackEnabled, cacheEnabled, cookiesEnabled) dan terapkan ke WebView. Field `cachedSettings` ditambahkan di `OverlayService` supaya settings terbaca sekali saat `onCreate()` dan di-update setiap kali berubah (lewat `setBubbleEnabled`, `exitService`, `clearAndExit`).
- **Wire up `OverlayConfig.keepPosition`** — field ini sudah ada di model tapi tidak pernah di-read. Sekarang di `OverlayWindow.create()`: kalau `keepPosition = false`, overlay mengabaikan `positionX`/`positionY` yang tersimpan dan langsung pakai default (100, 100), sehingga overlay selalu muncul di posisi awal alih-alih posisi terakhir yang disimpan. Berguna kalau user tidak mau overlay-nya "lompat" ke posisi lama saat service restart.
- Catatan: `alwaysOnTop` di `OverlayConfig` tetap tidak di-wire karena `TYPE_APPLICATION_OVERLAY` di Android SELALU on-top — tidak ada varian window type yang bisa jadi "sometimes on top" untuk overlay system window. Field ini dipertahankan di model untuk kompatibilitas JSON storage yang sudah ada.

# 42
# 13-09-2026-0914-WIB
- **Fix build error**: `processDebugMainManifest FAILED` (`ManifestMerger2$MergeFailureException` gagal parse `AndroidManifest.xml`). Penyebabnya XML comment TIDAK BOLEH berisi `--` (dua strip) di mana pun kecuali persis di penutup `-->` — komentar yang kutambahkan di changelog #41 buat permission `PACKAGE_USAGE_STATS` kepakai " -- " sebagai tanda pisah kalimat (gaya penulisan biasa), yang ternyata bikin manifest merger Android gagal parsing filenya sama sekali. Diganti jadi ":" biasa. Sudah ditelusuri ulang seluruh file XML yang disentuh (manifest + drawable ikon lain) buat mastiin tidak ada `--` tersisa di dalam badan comment manapun.

# 41
# 12-09-2026-2128-WIB
- **Lengkapi fitur "Simpan Posisi Sekarang" jadi beneran per-aplikasi** (App Layout), berdasarkan referensi proyek `FloatingDonationV20` yang dikasih user (khususnya `ForegroundAppWatcher.kt` & pola sinkronisasi profil-per-app di `OverlayService.kt`-nya). Sebelum ini, model (`AppLayout.kt`), storage CRUD (`OverlayStorage.kt`), dan UI kelola manual (`AppLayoutsScreen.kt`, `AppLayoutDialog.kt`) ternyata SUDAH ada dari sebelumnya, tapi murni dekorasi — tidak ada satupun kode yang mendeteksi aplikasi lagi di depan, jadi App Layout yang dibuat lewat UI itu tidak pernah otomatis kepakai.
- **`util/ForegroundAppWatcher.kt` (baru)** — di-port dari `FloatingDonationV20`, deteksi package aplikasi di foreground pakai `UsageStatsManager` (butuh izin "Usage Access" / `PACKAGE_USAGE_STATS`, yang cuma bisa diaktifkan manual lewat Settings sistem, bukan dialog izin biasa). Ada `hasUsageAccess()`, `getCurrentForegroundPackage()` (one-shot), dan `Watcher` (poller `Handler` tiap 1.2 detik, cuma trigger callback kalau package-nya beneran berubah).
- **`AndroidManifest.xml`** — tambah `<uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />`.
- **`overlay/OverlayWindow.kt`** — tambah `getLiveGeometry()` / `getLiveOpacity()` / `updateLiveOpacity()`. Ini krusial: `updatePositionAndSize()` yang sudah ada sebelumnya cuma mengubah window yang lagi tampil TANPA menyentuh `config`/storage (dipakai buat drag manual) — method baru ini dipakai dengan cara yang sama supaya perpindahan App Layout otomatis juga tidak pernah menimpa layout Default yang tersimpan. `getLiveGeometry()`/`getLiveOpacity()` dipakai buat baca geometri yang BENERAN lagi tampil di layar sekarang (bisa jadi hasil App Layout, bisa Default) — beda dari `getConfig()` yang selalu balikin Default apa adanya.
- **`service/OverlayService.kt`** (perubahan inti):
  - Cache `cachedAppLayouts` yang terus disinkronkan lewat `storage.getAppLayouts().collect { ... }` (bukan sekali baca) supaya perubahan dari layar "App Layouts" di MainActivity atau dari poin di bawah langsung kepakai tanpa restart Service.
  - `appWatcher` (`ForegroundAppWatcher.Watcher`) dimulai di `onCreate()`, distop di `onDestroy()`. Callback-nya (`onForegroundAppChanged`) mencocokkan App Layout yang aktif untuk package baru itu ke SETIAP overlay yang lagi nyala — kalau ketemu & `isEnabled`, pindahkan window ke posisi/ukuran/opacity App Layout itu; kalau tidak ada, pastikan window balik ke layout Default overlay.
  - `startOverlay()`: begitu window baru dibuat, langsung cek App Layout untuk aplikasi yang KEBETULAN sudah di depan saat itu (`applyLayoutForForegroundApp`) — supaya user yang nyalain overlay pas udah di tengah main game, overlay-nya langsung nongol di posisi App Layout game itu, bukan Default dulu baru lompat.
  - `saveCurrentPositions()` ("Simpan Posisi Sekarang" di menu bubble) dirombak total: kalau aplikasi yang di depan LAYAR SEKARANG bukan aplikasi ini sendiri → otomatis bikin (kalau belum ada) atau update App Layout untuk aplikasi itu dengan geometri LIVE yang lagi ditampilkan (termasuk auto-isi nama aplikasi lewat `PackageManager`, dan auto-`isEnabled = true` — karena "simpan" secara implisit berarti "pakai layout ini"). Kalau tidak ada aplikasi lain terdeteksi (izin belum aktif) atau yang di depan adalah aplikasi ini sendiri → tetap simpan ke layout Default overlay seperti sebelumnya, jadi behavior lama tidak rusak buat orang yang belum ngasih izin Usage Access.
- **`ui/screens/AppLayoutsScreen.kt`** — tambah `UsageAccessBanner()`: kartu peringatan (warna `errorContainer`) + tombol "Aktifkan Izin" yang buka `Settings.ACTION_USAGE_ACCESS_SETTINGS`, muncul di atas daftar (baik pas kosong maupun sudah terisi) kalau `ForegroundAppWatcher.hasUsageAccess()` masih `false`, supaya user ngerti kenapa App Layout yang mereka buat belum jalan otomatis.
- Desain penting yang dijaga di seluruh perubahan ini: App Layout TIDAK PERNAH berlaku untuk package aplikasi ini sendiri (`resolveAppLayout` return null kalau `pkg == packageName`), dan perpindahan otomatis (baik App Layout maupun balik ke Default) SELALU lewat jalur live-window (`updatePositionAndSize`/`updateLiveOpacity`), TIDAK PERNAH lewat `updateConfig()`/persist storage — supaya gonta-ganti aplikasi tidak pernah diam-diam menimpa layout Default yang sudah user atur.
- CATATAN: belum sempat compile beneran (tidak ada akses internet buat Gradle di environment ini) — sudah dicek manual kurung kurawal/kurung biasa seimbang di semua file yang diubah (`OverlayService.kt` 147/147, `OverlayWindow.kt` 90/90, `ForegroundAppWatcher.kt` 13/13, `AppLayoutsScreen.kt` 69/69, `MainActivity.kt` 156/156 — tidak berubah, cuma dicek ulang). Sangat disarankan build & tes skenario: (1) buka game/app lain, atur posisi overlay lewat drag, tap "Simpan Posisi Sekarang" di menu bubble → cek muncul entry baru otomatis di "App Layouts" overlay itu; (2) pindah ke app lain lagi (tanpa profil) → overlay harus balik ke posisi Default; (3) balik lagi ke app pertama tadi → overlay harus otomatis balik ke posisi yang disimpan tadi tanpa perlu diatur ulang; (4) coba tanpa izin Usage Access aktif dulu → pastikan tidak crash, cuma fitur auto-switch-nya yang tidak jalan & banner peringatan muncul di layar App Layouts.

# 40
# 12-09-2026-1533-WIB
- **Ketemu akar masalah SEBENARNYA** dari bug "tap posisi terakhir setelah Sembunyikan Bubble tidak muncul lagi" — fix alpha-only di changelog #39 ternyata tidak cukup. Runtutannya: tap bubble untuk buka menu → `OverlayService.showBubbleMenu()` manggil `bubble.hide()` (`visibility = GONE`) buat nyembunyiin bubble selagi menunya tampil. Lalu waktu user pilih "Sembunyikan Bubble" DI DALAM menu itu, jalurnya cuma `bubbleMenu?.dismiss()` (nutup window menu doang) lalu `hideAsGhost()` — **TIDAK PERNAH lewat `collapseBubbleMenu()`/`show()` yang biasanya mengembalikan `visibility` ke `VISIBLE`**. Jadi `hideAsGhost()` versi #39 (cuma `alpha = 0f`) dipanggil saat `bubbleView` MASIH `GONE` sisa dari `hide()` di awal — window yang `GONE` tidak pernah di-hit-test sama sekali, alpha mau 0 atau berapa pun gak ngaruh, tap-nya tidak pernah sampai ke `unghost()`. Ini kenapa bug-nya kelihatan "gak kefix" walau fix alpha kemarin secara konsep sudah benar.
- Fix di `FloatingBubble.kt`: `hideAsGhost()` sekarang eksplisit set `visibility = View.VISIBLE` DULU baru `alpha = 0f` (bukan cuma alpha doang) — supaya jalur manapun yang manggilnya (baik saat visibility udah VISIBLE maupun masih GONE peninggalan buka-menu), hasil akhirnya selalu konsisten: visible-secara-status + transparan-secara-visual. `unghost()` juga ditambah `visibility = View.VISIBLE` yang sama sebagai jaga-jaga (idempotent, gak masalah dipanggil walau statusnya udah visible).
- CATATAN: sudah ditelusuri manual seluruh rantai pemanggilannya (`showBubbleMenu()` -> `hide()` -> aksi "Sembunyikan Bubble" -> `hideAsGhost()` -> tap -> `unghost()`) buat mastiin gak ada jalur lain yang ninggalin `visibility` di state yang salah. Kurung kurawal & kurung biasa dicek seimbang (49/49). Tetap belum sempat compile beneran di environment ini (tidak ada akses Gradle/internet) — mohon build & tes ulang skenario yang sama persis: tap bubble buka menu -> "Sembunyikan Bubble" -> bubble transparan penuh & gak bisa digeser -> tap posisi terakhir -> bubble WAJIB muncul lagi kali ini.

# 39
# 12-09-2026-1525-WIB
- **Fix lanjutan changelog #38**: dilaporkan user tap di posisi terakhir bubble setelah "Sembunyikan Bubble" tidak memunculkan apa-apa. Penyebabnya: `hideAsGhost()` kemarin pakai `bubbleView.visibility = View.INVISIBLE` — ternyata untuk `bubbleView` yang posisinya sebagai ROOT content langsung dari window-nya sendiri (ditambahkan lewat `WindowManager.addView()`, bukan child di dalam ViewGroup lain), window jadi berhenti nerima sentuhan sama sekali begitu `visibility` bukan `VISIBLE` — jadi tap di posisi itu tidak sampai ke `setOnTouchListener` sama sekali, `unghost()` tidak pernah kepanggil.
- Fix: `hideAsGhost()`/`unghost()` (`FloatingBubble.kt`) diganti total pakai `alpha` alih-alih `visibility`: `alpha = 0f` buat nyembunyiin (visual transparan 100%, tapi `visibility` tetap `VISIBLE` terus sepanjang waktu), `alpha = config.opacity` buat munculin lagi. Karena `visibility` tidak pernah diubah sama sekali selama ghost, window dijamin tetap menerima sentuhan di posisi yang sama persis — trik standar Android buat "invisible tapi tetap interaktif" (beda dari `INVISIBLE`/`GONE` yang keduanya bikin masalah touch-dispatch kalau dipakai di root view sebuah window).
- `show()` juga disesuaikan supaya ikut restore `alpha = config.opacity` (bukan cuma `visibility = VISIBLE`), jaga-jaga kalau dipanggil sehabis state ghost.
- CATATAN: sama seperti sebelumnya, tidak sempat compile beneran di environment ini — sudah dicek manual (kurung seimbang) tapi mohon build & tes ulang persis skenario yang dilaporkan: Sembunyikan Bubble -> tap posisi terakhir -> bubble WAJIB muncul lagi kali ini.

# 38
# 12-09-2026-1517-WIB
- **Perbaikan fitur "Sembunyikan Bubble" di floating menu.** Sebelumnya aksi ini manggil `setBubbleEnabled(false)` — itu SALAH secara konsep: bikin bubble beneran mati (window di-destroy + `bubbleEnabled` ke-persist `false` ke storage, sama kayak matiin dari Dashboard), padahal maksud "sembunyikan" harusnya cuma nyembunyiin tampilannya doang, bukan mematikannya.
- Ditambahkan mode "ghost" khusus di `FloatingBubble.kt`: method baru `hideAsGhost()` bikin window bubble jadi `View.INVISIBLE` (BUKAN `View.GONE` seperti `hide()` yang sudah ada sebelumnya untuk transisi buka-menu) di posisi terakhirnya — window-nya sendiri TIDAK di-destroy dan `bubbleEnabled` TIDAK disentuh sama sekali (tetap `true`/on). `View.INVISIBLE` dipilih secara sengaja (bukan `GONE`) karena window bubble ini adalah root view dari window-nya sendiri (bukan child di dalam ViewGroup lain) — jadi tetap menerima sentuhan di posisi yang sama walau tidak digambar, beda dengan child biasa di ViewGroup yang visibility-nya mempengaruhi hit-testing dari sisi parent.
- **Posisi dikunci total selama ghost**: `ACTION_MOVE` di `setupTouchHandling()` sekarang di-skip total kalau `isGhosted == true`, jadi bubble tidak bisa ke-geser sama sekali selama disembunyikan (persis seperti yang diminta: "menglock posisi bubble agar ga geser").
- **Tap di posisi terakhir langsung memunculkannya lagi**: `ACTION_UP` sekarang cek `isGhosted` duluan — kalau true, langsung panggil `unghost()` (balik ke `View.VISIBLE`) TANPA membuka menu bubble (beda dari tap normal yang manggil `onBubbleClicked()`). Jadi alurnya: tap 1x di titik terakhir = bubble muncul lagi; mau buka menu, tap sekali lagi seperti biasa.
- `OverlayService.kt`: aksi "Sembunyikan Bubble" di `showBubbleMenu()` diganti dari `setBubbleEnabled(false)` jadi `floatingBubble?.hideAsGhost()`.
- CATATAN: dicek manual (kurung kurawal & kurung biasa seimbang di kedua file yang diubah), tapi tetap tidak sempat compile beneran di environment ini (tidak ada akses Gradle/internet) — mohon build & tes: buka menu bubble → "Sembunyikan Bubble" → bubble harus hilang dari layar TAPI switch "Floating Bubble" di Dashboard harus tetap ON (karena statusnya memang masih on, cuma disembunyikan) → tap di posisi terakhir bubble → bubble harus muncul lagi persis di situ, tanpa geser dan tanpa nampilin menu.

# 37
# 12-09-2026-1504-WIB
- **FIX BUG: "Stop Semua" di floating menu bikin switch "Floating Bubble" di Dashboard nyangkut ON padahal bubble aslinya sudah mati.** Akar masalah di `OverlayService.kt`: aksi "Stop Semua" pada `BubbleMenu` (menu 6-item saat bubble ditekan) manggil `exitService()`, dan fitur "Clear & Exit" di Settings manggil `clearAndExit()` — dua-duanya sudah benar mematikan window bubble (`destroyBubble()`), TAPI tidak pernah nulis ulang `AppSettings.bubbleEnabled = false` ke storage. Karena switch "Floating Bubble" di Dashboard sumbernya dari `settings.bubbleEnabled` (persisted), begitu bubble mati lewat 2 jalur ini, nilai `bubbleEnabled` di storage tetap `true` selamanya — bukan sekadar tampilan yang perlu di-refresh (makanya pindah menu ke Settings lalu balik lagi ke Dashboard TIDAK memperbaikinya, karena datanya sendiri yang salah, bukan cache UI basi).
- Fix: `exitService()` dan `clearAndExit()` sekarang sama-sama nge-set `bubbleEnabled = false` (in-memory) dan persist lewat `storage.saveSettings(settings.copy(bubbleEnabled = false))`, persis pola yang sudah dipakai di `setBubbleEnabled()`. Sekarang "Stop Semua" dari floating menu (dan "Clear & Exit" dari Settings) bikin switch "All Overlays" DAN "Floating Bubble" di Dashboard sama-sama ikut ke-OFF dengan benar, konsisten mau diklik dari dalam aplikasi maupun saat aplikasi lain lagi di foreground.
- CATATAN: dicek manual (kurung kurawal & kurung biasa seimbang di kedua fungsi yang diubah), tapi tetap tidak sempat compile beneran di environment ini (tidak ada akses Gradle/internet) — mohon build & tes ulang: buka bubble menu, tap "Stop Semua", lalu cek Dashboard — switch "All Overlays" & "Floating Bubble" harus dua-duanya OFF, dan tetap OFF setelah pindah ke Settings lalu balik lagi.

# 36
# 11-09-2026-1807-WIB
- Build sukses (dari log user tombol "Buka Semua"/"Matikan Semua" sudah sejajar), TAPI garis slider transparansi masih tidak muncul sama sekali di screenshot terbaru (cuma bulatan thumb doang) — padahal thumb-nya sendiri sudah render dengan benar di posisi yang tepat sesuai persentase (75% vs 100% beda posisi X, jadi progress-nya jalan, cuma track-nya yang invisible). Ini nunjukkin masalahnya BUKAN di logika progress, tapi murni di rendering `progressDrawable`-nya — dan null-in tint list di changelog #34 ternyata belum cukup (kemungkinan behaviour tint reset ini beda-beda tiap OEM/tema, tidak konsisten).
- **GANTI PENDEKATAN TOTAL (bukan tambal lagi)**: `sliderRow()`/`styleSeekBar()` di `OverlayPanels.kt` dirombak — track sekarang BUKAN lagi `progressDrawable` bawaan `SeekBar` (`LayerDrawable`+`ClipDrawable`, sumber masalah dari changelog #32-#35), tapi 2 `View` polos yang ditumpuk di `FrameLayout` DI BAWAH `SeekBar`:
  - `trackBg`: pill abu-abu transparan, lebar penuh (dikurangi inset 20dp kiri-kanan biar sejajar sama jangkauan gerak thumb) — selalu kelihatan, statis.
  - `trackFill`: pill warna aksen, lebar di-resize manual (`layoutParams.width`) tiap `onProgressChanged` sesuai persentase — DIHITUNG LANGSUNG dari lebar `trackBg` yang sudah di-layout, bukan lewat level/clip drawable.
  - `SeekBar`-nya sendiri sekarang cuma buat nangkep sentuhan + gambar thumb (yang dari awal SUDAH kelihatan benar di semua screenshot user) — `progressDrawable`-nya dibikin transparan total, jadi tidak ada lagi ketergantungan ke sistem tema/tint Android buat nampilin garis track. Karena `trackBg`/`trackFill` cuma `View` polos dengan `background` `GradientDrawable` manual, tidak ada mekanisme tint/level Android yang bisa "menimpa" warnanya lagi — dijamin tampil apa adanya di device manapun.
  - Import `ClipDrawable`/`LayerDrawable` (sudah tidak dipakai) dihapus dari `OverlayPanels.kt`.
- CATATAN: dicek manual (kurung seimbang, tidak ada sisa referensi ke fungsi `styleSeekBar` lama), tapi tetap tidak sempat compile beneran di environment ini — mohon build & screenshot ulang buat konfirmasi track-nya sudah kelihatan.

# 35
# 11-09-2026-1801-WIB
- Fix error compile dari perubahan #34 (`compileDebugKotlin` gagal, dilaporkan lewat build log user): parameter baru `textSizeSp`/`iconSizeDp` di `actionButton()` kemarin ditaruh SETELAH `onClick`, jadi `onClick` bukan lagi parameter terakhir — itu merusak semua pemanggilan trailing-lambda `actionButton(...) { ... }` yang sudah ada (Simpan, Tutup, Tambah & Aktifkan, Batal, Selesai), karena sintaks trailing lambda Kotlin cuma valid kalau parameter lambda ada di posisi terakhir. Fix: pindahkan `textSizeSp`/`iconSizeDp` ke SEBELUM `onClick` di signature-nya, `onClick` balik jadi parameter terakhir lagi. Pemanggilan "Buka Semua"/"Matikan Semua" (yang pakai named argument `onClick = { ... }`) tidak perlu diubah karena named argument tidak tergantung posisi.
- CATATAN: dicek manual (kurung kurawal & kurung biasa seimbang, semua 7 call site `actionButton()` ditelusuri satu-satu), tapi tetap tidak sempat compile beneran di environment ini (tidak ada Gradle/internet) — mohon build ulang & konfirmasi.

# 34
# 11-09-2026-1756-WIB
- Lanjutan dari changelog #33 — user masih screenshot garis slider hilang total (cuma bulatan putih doang) meski thickness/alpha/urutan assign sudah dibenerin di #33. `OverlayPanels.kt`:
- **FIX AKAR MASALAH slider (bukan cuma kosmetik lagi)**: ternyata sejak API 21, style bawaan `SeekBar` (`Widget.Material.SeekBar`) otomatis pasang `progressTintList`/`progressBackgroundTintList`/`thumbTintList` sendiri (diambil dari tema, `colorControlActivated`/`colorControlNormal`) dan nge-tint drawable custom kita pakai `PorterDuff.Mode.SRC_IN` — mode ini MENGGANTI warna RGB drawable kita dengan warna tema, cuma nyisain alpha kita sebagai mask. Jadi track abu-abu & progress ungu yang sudah di-set manual di `styleSeekBar()` ketiban tint tema di atasnya dan jadi nyaris tak kelihatan — bukan soal tebal/opacity/urutan lagi. Fix: null-in ketiga tint list (+ tint mode) di `styleSeekBar()` setelah drawable & thumb custom di-assign, supaya warna yang sudah diset di atas benar-benar tampil apa adanya.
- **FIX tombol "Buka Semua"/"Matikan Semua" tidak sejajar**: "Matikan Semua" (teks lebih panjang) turun jadi 2 baris di dalam pill setengah-lebar yang sama, bikin tombolnya lebih tinggi daripada "Buka Semua" di sebelahnya (kelihatan di screenshot — baris teks "Matikan"/"Semua" bertumpuk). `actionButton()` sekarang terima parameter opsional `textSizeSp`/`iconSizeDp` + teksnya dipaksa `isSingleLine = true` (dengan ellipsize sebagai jaga-jaga). Kedua tombol bulk action di panel Kustomisasi Overlay sekarang pakai ukuran teks/ikon lebih kecil (13sp/16dp, dari default 14sp/18dp) supaya "Matikan Semua" pasti muat 1 baris dan tingginya sama persis dengan "Buka Semua"; `bulkRow` juga ditambah `gravity = Gravity.CENTER_VERTICAL` sebagai jaga-jaga tambahan.
- CATATAN: belum sempat di-compile langsung (tidak ada akses internet buat Gradle di environment ini) — sudah dicek manual (kurung kurawal & kurung biasa seimbang, semua call site `actionButton()` lain masih pakai trailing lambda jadi tetap kompatibel dengan parameter baru), tapi tetap disarankan build & coba langsung di device/emulator.

# 33
# 11-09-2026-1630-WIB
- Lanjutan dari changelog #32 — sekarang MENYELURUH ke semua bagian floating bubble (bukan cuma panel Kustomisasi Overlay), sesuai screenshot terbaru dari user:
- **File baru `overlay/DynamicColors.kt`**: memindahkan logika resolusi dynamic color (yang sebelumnya cuma ada di `BasePanel`/`OverlayPanels.kt`) jadi 4 fungsi top-level yang dipakai bareng oleh SEMUA file floating-UI — `resolveAccentColor()`, `resolveSecondaryColor()`, `resolveDangerColor()` (baca `android.R.color.system_accent1/2/3_200`, fallback ke hex lama di Android < 12), dan `resolveTonal()` (helper opacity). `BasePanel` di `OverlayPanels.kt` sekarang tinggal delegasi ke fungsi-fungsi ini (duplikat kodenya dihapus).
- **`BubbleMenu.kt`** (menu 6-item yang muncul saat bubble ditekan) — sebelumnya KELEWAT waktu ngerjain dynamic color di changelog #32 karena filenya beda sama `OverlayPanels.kt`:
  - Drag handle "∷ Geser" diganti jadi pill (sama persis kayak di panel-panel lain).
  - Warna ikon (sebelumnya hardcode `#9C7BFF`) dan backdrop bulat di belakang tiap ikon (sebelumnya hardcode `Color.argb(30,156,123,255)`, RGB dari `#9C7BFF` yang sama) sekarang pakai `resolveAccentColor()`/`resolveTonal()`.
- **`FloatingBubble.kt`** (bubble itu sendiri, sebelum ditekan) — background bulatnya sebelumnya `bubble_background.xml`, drawable statis warna `#6200EE` ungu tetap. Sekarang dibangun langsung lewat kode (`GradientDrawable` oval) pakai `resolveAccentColor()`, jadi ikut wallpaper juga kayak menu & panel-panelnya. Drawable XML lama dibiarkan ada di `res/drawable` (tidak dipakai lagi, tidak masalah dibiarkan).
- **FIX lanjutan slider transparansi**: dari screenshot terbaru, garis track slider (baik yang abu-abu maupun yang warna aksen) masih nyaris tidak kelihatan meskipun bug lebar/kepotong dari changelog #32 sudah beres. Ternyata masih ada 2 masalah tambahan di `styleSeekBar()`/`sliderRow()` (`OverlayPanels.kt`):
  1. Tinggi track efektif sebelumnya cuma ~2dp (padding vertikal 8dp + inset layer 9dp dari total tinggi SeekBar 36dp, nyisain nyaris 0dp buat track-nya) — dinaikkan jadi 8dp (ubah total tinggi SeekBar 36dp->32dp, padding vertikal 8dp->4dp, inset dihitung ulang biar hasil akhirnya persis 8dp track tebal & jelas).
  2. Opacity track abu-abu (background, bagian yang BELUM ke-drag) sebelumnya cuma 30% — dinaikkan ke 70% biar kontras kelihatan di atas card.
  3. `seek.progress = ...` sebelumnya di-set SEBELUM `progressDrawable` di-assign (`styleSeekBar()` dipanggil belakangan) — urutan dibalik jadi assign drawable dulu baru set progress, supaya level clip drawable buat warna aksen ke-sync dengan benar sejak awal render (bukan cuma pas user gesernya).

# 32
# 11-09-2026-1600-WIB
- Perbaikan panel "Kustomisasi Overlay" (dan 2 panel lain: Pengaturan Bubble, Tambah Overlay Baru) sesuai screenshot yang dikasih user — semua di `OverlayPanels.kt`:
- **Drag handle jadi pill**: teks "∷ Geser" di header setiap panel diganti jadi bentuk pill polos (36dp x 4dp, putih transparan) tanpa teks/ikon — pola grab-handle bottom sheet standar, sama seperti yang pernah dipakai di `BubbleMenu` (changelog #28). Area sentuh drag-nya tetap sama (masih seluruh strip header 26dp), cuma tampilannya yang berubah.
- **FIX BUG UTAMA slider "kepotong"**: akar masalahnya ternyata SeekBar di `sliderRow()` sebelumnya ditambahkan ke parent-nya TANPA `LayoutParams` sama sekali, jadi otomatis jatuh ke lebar `WRAP_CONTENT` dan menciut jadi secuil kecil pas nempel di sebelah ikon kunci/hapus (persis kelihatan di screenshot user — cuma keliatan potongan bulatan putih doang, bukan slider penuh). Sekarang lebar SeekBar di-set eksplisit `MATCH_PARENT` (+ tinggi tetap 36dp) supaya konsisten mengisi ruang yang tersedia di row-nya. Selain itu `setPadding(0,0,0,0)` (yang sengaja mematikan ruang cadangan buat bulatan thumb di kedua ujung slider) diganti jadi padding horizontal 10dp (>= radius thumb) supaya bulatan thumb tidak lagi kepotong separuh waktu ada di posisi 0% atau 100%. Ukuran thumb juga dinaikkan sedikit (18dp->20dp) dan radius pill track dirapikan (5dp->3dp) biar makin jelas kelihatan.
- **Dynamic color (Material You)**: seluruh warna aksen yang sebelumnya hardcode hex (`#9C7BFF` ungu, `#FFC107` kuning buat kunci, `#FF8A80`/`#FF8A65` merah/oranye buat hapus & "Matikan Semua", `#6A4FC1` buat preview bubble) sekarang diambil dari palet sistem Android 12+ (`android.R.color.system_accent1/2/3_200`, mengikuti warna wallpaper user) lewat 3 property baru di `BasePanel`: `accentColor` (aksi utama: Simpan/Tambah&Aktifkan/Selesai/Buka Semua, switch aktif, slider, ikon judul section), `secondaryColor` (tonal kedua: chip kunci, tombol Tutup/Batal), `dangerColor` (tonal ketiga, beda hue: chip hapus, tombol "Matikan Semua"). Di Android < 12 (API < 31, dynamic color belum ada di OS-nya), otomatis fallback ke hex lama supaya tetap ada warna yang masuk akal.
- CATATAN: sama seperti perubahan-perubahan sebelumnya di file ini, belum sempat di-compile langsung (tidak ada akses internet buat Gradle di environment ini) — sudah dicek manual (kurung kurawal & kurung biasa seimbang, semua referensi warna lama sudah tergantikan, tidak ada sisa `Color.parseColor(ACCENT/DANGER/WARNING)` yang kelewat) tapi tetap disarankan build & coba langsung di device/emulator buat mastiin dynamic color-nya kebaca bener dan slider-nya sudah kelihatan penuh.

# 31
# 11-09-2026-1539-WIB
- Merombak tampilan floating menu & ketiga panel (Pengaturan Bubble, Tambah Overlay Baru, Kustomisasi Overlay) dari changelog #29/#30 supaya lebih "enak dilihat" dan konsisten dengan gaya Material 3 Expressive (M3E): sudut lebih membulat, warna tonal (fill transparan dari warna aksen) buat tombol/chip alih-alih teks polos, dan slider bergaya pill.
- `bubble_menu_background.xml`: radius sudut dinaikkan dari 14dp jadi 26dp, ditambah outline tipis 1dp putih transparan (`#1FFFFFFF`) biar card lebih "pop" di atas background gelap — dipakai bareng oleh `BubbleMenu` & semua panel di `OverlayPanels.kt` jadi otomatis ke-apply ke semuanya.
- `OverlayPanels.kt` — helper baru/diubah:
  - `actionButton()` sekarang punya parameter `filled: Boolean`: kalau `true`, row-nya dapat background pill rounded tonal (radius besar, isi 36% opacity dari warna aksen) — dipakai buat aksi utama (Simpan, Tambah & Aktifkan, Selesai, Buka Semua, Matikan Semua). Return type diubah dari `TextView` ke `LinearLayout` karena sekarang bisa berisi ikon+teks sekaligus.
  - `iconChip()` (baru): ikon bulat kecil dengan background tonal (lock & delete di list Kustomisasi Overlay sekarang pakai ini, ganti dari ikon polos tanpa background).
  - `styleSeekBar()` (baru): bikin slider custom — track pill abu-abu transparan + progress pill warna aksen (dibangun dari `LayerDrawable`+`ClipDrawable`, bukan cuma warna hijau bawaan sistem yang sebelumnya nongol karena SeekBar tidak di-theming sama sekali) + thumb bulat putih ber-ring warna aksen. Dipanggil otomatis dari `sliderRow()` yang sekarang punya parameter `accent` (default ungu `#9C7BFF`).
  - `styleSwitch()` (baru): switch Aktif/Nonaktif di Kustomisasi Overlay ditinting manual (`trackTintList`/`thumbTintList`, API bawaan `Switch` sejak API 23 — tidak perlu tambah dependency `appcompat`) karena sebelumnya `Switch` polos tanpa tint render sebagai bulatan putih doang tanpa track yang kelihatan (persis kayak di screenshot yang dikasih user).
- Kustomisasi Overlay dirombak layout-nya: tiap overlay sekarang jadi CARD terpisah (rounded 16dp, fill putih transparan tipis) alih-alih cuma dipisah garis divider — baris 1 nama+switch, baris 2 slider transparansi + chip kunci + chip hapus sejajar (sebelumnya kunci/hapus nempel di baris nama, sekarang lebih lega). "Buka Semua Overlay"/"Matikan Semua Overlay" diringkas jadi "Buka Semua"/"Matikan Semua" dan ditaruh SEJAJAR (2 kolom chip, bukan ditumpuk vertikal) biar hemat tempat & terasa seperti pasangan aksi, bukan 2 menu terpisah.
- Fix bug tampilan dari screenshot user: area scroll daftar overlay sebelumnya SELALU setinggi 45% layar meskipun isinya cuma 4 item pendek — nyisain kotak kosong nganggur di bawah item terakhir sebelum tombol "Selesai". Sekarang tinggi scroll-nya di-cap SETELAH tinggi konten asli diketahui (`capScrollHeight()`, dipanggil lewat `listContainer.post{}` supaya nunggu layout pass selesai): kalau kontennya lebih pendek dari batas 45% layar, tinggi `ScrollView` dibiarkan `WRAP_CONTENT` (ngikutin isi asli, tidak ada spasi kosong); baru di-cap ke 45% layar kalau overlay-nya banyak. Perubahan tinggi ini dipush ke window lewat `requestResize()` (baru, wrapper `windowManager.updateViewLayout()`) karena window `WindowManager` ber-`WRAP_CONTENT` tidak otomatis resize sendiri cuma dari `requestLayout()` biasa.
- `BubbleMenu.kt`: ikon tiap baris menu sekarang dikasih backdrop bulat tonal ungu (bukan cuma ikon putih polos ditempel), warna ikon jadi ungu aksen `#9C7BFF` biar senada sama panel-panel. Tinggi baris dinaikkan 42dp->46dp dan lebar card 240dp->250dp buat kasih napas lebih.
- CATATAN: sama seperti sebelumnya, tidak sempat compile beneran (tidak ada akses internet buat Gradle di environment ini) — sudah direview manual + cek kurung kurawal/kurung biasa seimbang di semua file yang diubah, tapi tetap disarankan build & cek langsung di Android Studio.

# 30
# 11-09-2026-1514-WIB
- Mengganti SEMUA emoji/emoticon di floating menu & panel-panel dari changelog #29 (💾🫧🆕🎚️🙈🛑🔓🔌🗑✔✕) jadi ikon vektor bergaya Material Symbols (M3E), konsisten sama ikon yang sudah ada (`ic_lock`, `ic_show`, `ic_hide`, dll — 24x24, single path, tint putih lewat kode).
- Menambahkan 8 drawable ikon baru: `ic_save` (Simpan Posisi Sekarang), `ic_tune` (Kustomisasi Overlay, ganti 🎚️), `ic_add_circle` (Tambah Overlay Baru, ganti 🆕), `ic_stop_circle` (Stop Semua, ganti 🛑), `ic_check` (checkmark tombol Simpan/Tambah & Aktifkan/Selesai, ganti ✔), `ic_close` (tombol Tutup/Batal, ganti ✕), `ic_delete` (hapus overlay, ganti 🗑), `ic_power_off` (Matikan Semua Overlay, ganti 🔌). Untuk "Pengaturan Bubble" (ganti 🫧) dan "Sembunyikan Bubble" (ganti 🙈) pakai ulang ikon yang SUDAH ADA (`ic_bubble` dan `ic_hide`/visibility_off) karena kebetulan sudah pas secara makna, tidak perlu bikin baru. "Buka Semua Overlay" (ganti 🔓) juga pakai ulang `ic_unlock` yang sudah ada.
- `BubbleMenuAction` (`BubbleMenu.kt`): field `emoji` dihapus total, tinggal `iconRes` saja. `OverlayService.showBubbleMenu()` di-update supaya 6 action-nya pakai `iconRes` (bukan lagi string emoji).
- `OverlayPanels.kt`: helper `sectionTitle()` dan `actionButton()` ditulis ulang — sebelumnya cuma `TextView` dengan karakter emoji ditempel di depan teks lewat string interpolation, sekarang jadi row `LinearLayout` (ImageView ikon + TextView label) yang di-tint sesuai warna tombol. Ikon kunci/hapus per-overlay di panel "Kustomisasi Overlay" juga diganti dari `TextView` emoji (🗑) jadi `ImageView` (`ic_delete`, tint merah muda) supaya konsisten.
- Efek samping dari perubahan return type `actionButton()` (dari `TextView` ke `LinearLayout`, karena sekarang bisa berisi ikon+teks): dicek ulang semua pemanggilnya yang sempat nge-chain `.apply { gravity = ... }` di hasilnya (tombol "Selesai") — masih valid karena `LinearLayout` juga punya property `gravity`.

# 29
# 11-09-2026-1500-WIB
- Menambahkan fitur "Kustomisasi Overlay" lengkap ke floating menu, sesuai mockup yang diberikan user (gambar menu utama + 3 panel turunannya). Daftar menu utama di `BubbleMenu` (yang muncul saat bubble ditekan) diganti total dari 4 item lama (Show/Hide Overlays, Lock/Unlock Overlays, Open App, Exit) jadi 6 item baru: "💾 Simpan Posisi Sekarang", "🫧 Pengaturan Bubble", "🆕 Tambah Overlay Baru", "🎚️ Kustomisasi Overlay", "🙈 Sembunyikan Bubble", dan "🛑 Stop Semua". Header strip drag-handle juga diganti dari pill polos (changelog #28) jadi teks "∷ Geser" sesuai referensi.
- File baru `overlay/OverlayPanels.kt` berisi 3 floating window baru (masing-masing draggable sendiri lewat handle "∷ Geser", sama seperti `BubbleMenu`), dibangun di atas `BasePanel` (abstract, internal) yang menangani window-attach/drag/dismiss bersama:
  - `BubbleSettingsPanel` ("Pengaturan Bubble"): preview lingkaran bubble live (ukuran & transparansi ikut berubah saat slider digeser), slider "Ukuran" (30–120dp) dan "Transparansi" (20–100%), tombol Simpan/Tutup.
  - `AddOverlayPanel` ("Tambah Overlay Baru"): input Nama overlay + Link overlay (divalidasi pakai `UrlValidator` yang sudah ada, error ditampilkan lewat `Toast`), slider Transparansi, tombol "Tambah & Aktifkan" (langsung `startOverlay()`) / Batal.
  - `OverlayCustomizationPanel` ("Kustomisasi Overlay"): daftar semua overlay tersimpan (scrollable) dengan per-item switch Aktif/Nonaktif, ikon lock (toggle terkunci/tidak), ikon hapus (🗑), dan slider transparansi sendiri-sendiri; plus aksi massal "🔓 Buka Semua Overlay" (unlock semua) dan "🔌 Matikan Semua Overlay" (stop semua overlay yang aktif), serta tombol "✔ Selesai".
- Panel-panel ini pakai widget Android biasa (EditText/SeekBar/Switch/ScrollView), BUKAN Compose, karena dirender lewat `WindowManager` langsung dari `OverlayService` (bukan dari Activity) — konsisten dengan pendekatan `BubbleMenu`/`FloatingBubble` yang sudah ada. Beda dari `BubbleMenu`, window panel ini TIDAK pakai `FLAG_NOT_FOCUSABLE` supaya EditText & keyboard bisa dipakai.
- `OverlayService.kt`: `showBubbleMenu()` ditulis ulang untuk actions baru; ditambah `showBubbleSettingsPanel()`, `showAddOverlayPanel()`, `showOverlayCustomizationPanel()`, `saveCurrentPositions()` (flush posisi bubble + semua overlay aktif ke storage), dan `stopAllOverlays()` (stop semua overlay yang lagi jalan). Ditambah `collapseToBubble(x, y)` — versi `collapseBubbleMenu()` yang terima koordinat eksplisit, dipakai buat balik ke bubble dari sub-panel (posisi diambil dari `panel.getPosition()` supaya tetap akurat walau panelnya sempat digeser sebelum ditutup). `destroyBubble()` ikut di-update supaya dismiss ketiga panel baru juga, tidak cuma `bubbleMenu`, biar tidak ada window yang ke-leak.
- Item menu "Open App" dihapus dari floating menu (tidak ada di mockup baru) — import `MainActivity` yang jadi tidak terpakai ikut dibuang dari `OverlayService.kt`.
- Selama pengerjaan sempat ketemu & diperbaiki beberapa bug Kotlin di `OverlayPanels.kt` sebelum sempat nyangkut: (1) `BasePanel` awalnya `private` padahal di-`extend` sama class `public` (`BubbleSettingsPanel` dkk) — tidak valid, Kotlin melarang public class expose private supertype, jadi `BasePanel` (dan ketiga subclass-nya) diubah jadi `internal`. (2) beberapa parameter fungsi helper (`text`, `hint`, `inputType`, `max`) namanya sama persis dengan property bawaan `View`/`SeekBar` (`TextView.text`, `EditText.hint`, `EditText.inputType`, `SeekBar.max`) — di dalam blok `.apply { }`, referensi tanpa qualifier ke nama itu ke-shadow oleh property si view sendiri (bukan ambil dari parameter), jadi kalau tidak diganti nama hasilnya salah secara diam-diam (mis. `this.text = text` malah baca `text` kosong dari TextView itu sendiri, bukan dari argumen). Semua parameter yang bentrok di-rename (`label`, `hintText`, `fieldInputType`, `rangeMin`/`rangeMax`) supaya tidak ambigu.
- CATATAN: proyek tidak sempat di-compile beneran (Gradle butuh download distribusi ~lewat internet, tidak tersedia di environment ini) — perubahan sudah direview manual baris-per-baris (termasuk cek kurung kurawal/kurung biasa seimbang) tapi belum diverifikasi lewat build asli. Disarankan langsung build & jalanin di Android Studio buat mastiin tidak ada yang kelewat.

# 28
# 11-09-2026-0915-WIB
- Mengganti lagi header `BubbleMenu.kt` dari changelog #27 (ikon bubble + chevron-down) karena masih terlihat aneh: row-nya kosong-lebar dan chevron-down di menu yang sudah kebuka membingungkan (chevron-down biasanya berarti "buka/lihat lebih", bukan "tutup"). Diganti jadi GRAB-HANDLE minimalis: cuma satu pill kecil (32dp x 4dp, putih transparan) di tengah strip setinggi 28dp, tanpa ikon dan tanpa teks sama sekali — pola yang sudah familiar (mirip handle bottom sheet), jadi tidak perlu ikon/label buat dimengerti. Fungsinya tetap sama: tap strip ini balik ke bubble.
- Menghapus `ic_collapse.xml` (sudah tidak dipakai setelah diganti grab-handle).
- Sedikit merapikan action row (Show/Hide, Lock/Unlock, Open App, Exit): tinggi baris dikecilkan dari 44dp jadi 40dp, ukuran ikon dari 20dp jadi 18dp, padding kanan dikurangi dari 18dp ke 14dp supaya ruang kosong di sisi kanan tulisan pendek (mis. "Exit") tidak terlalu lebar/janggal.
- Menyesuaikan lagi perhitungan deteksi tap-per-baris (`setupTouchHandling`) dan fallback tinggi buat clamp drag, karena sekarang tinggi strip handle (`headerHeightPx`) beda dari tinggi baris aksi (`rowHeightPx`) — sebelumnya di changelog #27 keduanya disamakan.

# 27
# 11-09-2026-0900-WIB
- Mengubah style baris "Collapse" di `BubbleMenu.kt` yang dikeluhkan terlihat aneh sebagai row menu teks biasa. Sekarang baris paling atas jadi HEADER bar terpisah (ikon bubble kecil di kiri + ikon chevron-down "collapse" di kanan, tanpa teks "Collapse"), dipisahkan dari daftar aksi (Show/Hide, Lock/Unlock, Open App, Exit) di bawahnya oleh garis divider tipis — bukan lagi row polos bertuliskan "Collapse" yang membingungkan. Perilakunya sama persis: tap area header ini balik ke bentuk bubble.
- Menambahkan ikon baru `ic_collapse.xml` (chevron-down), dipakai khusus di header ini — dipilih chevron-down (bukan tanda silang "X") karena aksinya "kembali jadi bubble", bukan "keluar/tutup".
- Menyesuaikan perhitungan deteksi tap-per-baris di `BubbleMenu.setupTouchHandling()` supaya tetap akurat dengan adanya divider baru di antara header dan daftar aksi (sebelumnya asumsi semua baris tingginya seragam tanpa divider).

# 26
# 11-09-2026-0848-WIB
- Mengubah perilaku floating menu dari changelog #25: sebelumnya menu muncul sebagai popup di sebelah bubble sementara bubble-nya tetap kelihatan dan menu-nya statis (tidak bisa digeser). Sekarang saat bubble ditekan, bubble-nya HILANG (`FloatingBubble.hide()`) dan digantikan `BubbleMenu` yang jadi overlay window sendiri persis di posisi bubble — dan menu ini bisa DIGESER bebas seperti bubble (drag & drop pakai `WindowManager`, logika click-vs-drag sama seperti `FloatingBubble`).
- `BubbleMenu.kt` ditulis ulang total: bukan lagi full-screen transparent overlay, tapi window kecil `WRAP_CONTENT` yang isinya baris "Collapse" (kembali ke bubble) di paling atas + baris-baris aksi (Show/Hide, Lock/Unlock, Open App, Exit) di bawahnya. Deteksi tap-per-baris dihitung dari posisi Y sentuhan relatif terhadap tinggi tiap baris (bukan pakai `OnClickListener` per-child) karena seluruh card sekarang punya satu `setOnTouchListener` yang juga menangani drag.
- Menambahkan `FloatingBubble.moveTo(x, y)` supaya begitu menu di-collapse, bubble muncul lagi PERSIS di posisi terakhir menu digeser (bukan balik ke posisi lama sebelum menu dibuka), dan posisi barunya ikut tersimpan lewat `onConfigChanged` yang sudah ada.
- Menekan salah satu aksi (Show/Hide, Lock/Unlock, Open App, Exit) otomatis collapse balik ke bubble setelah aksinya jalan (`OverlayService.menuAction()`), jadi tidak perlu tap "Collapse" secara terpisah tiap kali habis pilih aksi.

# 25
# 11-09-2026-0835-WIB
- Merevisi fix bubble tap di changelog #24: ternyata yang dimaksud user bukan langsung toggle show/hide overlay, tapi memunculkan floating MENU saat bubble ditekan. Sekarang tap bubble memunculkan popup menu kecil di sebelah bubble (class baru `BubbleMenu.kt`), berisi 4 aksi cepat: "Show/Hide Overlays" (label & ikon berubah sesuai status), "Lock/Unlock Overlays" (idem), "Open App" (buka `MainActivity`), dan "Exit" (`exitService()`). Tap di luar area menu atau tap salah satu aksi otomatis menutup menu.
- `BubbleMenu` diimplementasikan sebagai overlay window full-screen transparan (`WindowManager`, `TYPE_APPLICATION_OVERLAY`) berisi card kecil yang posisinya mengikuti lokasi bubble saat ini (`FloatingBubble.getPosition()`/`getSizePx()`, method baru), otomatis pindah ke atas bubble kalau ruang di bawah tidak cukup. Reuse ikon yang sudah ada (`ic_show`, `ic_hide`, `ic_lock`, `ic_unlock`, `ic_exit`) dan drawable baru `bubble_menu_background.xml` (card gelap rounded) & `bubble_menu_item_background.xml` (highlight saat item ditekan).
- Variabel `isPanelVisible` (sudah tidak terpakai sejak changelog #24) dihapus, digantikan `bubbleMenu: BubbleMenu?` yang statusnya dicek lewat `isShowing()`. `destroyBubble()` (dipanggil juga dari `onDestroy()`) sekarang ikut dismiss menu supaya tidak ada overlay window yang ke-leak kalau bubble/service dimatikan saat menu masih terbuka.

# 24
# 11-09-2026-0828-WIB
- Memperbaiki bug floating bubble tidak memunculkan apa-apa saat ditekan (tap, bukan drag). Penyebabnya di `OverlayService.kt` (`createBubble()`): lambda `onBubbleClicked` cuma toggle variabel lokal `isPanelVisible` yang tidak pernah dipakai untuk apa pun, lalu memanggil callback publik `onBubbleClicked?.invoke()` yang ternyata TIDAK PERNAH di-assign dari mana pun di codebase (cuma dideklarasikan doang) — jadi tap bubble benar-benar tidak ada efeknya sama sekali. Diperbaiki dengan membuat tap bubble langsung toggle show/hide semua overlay (pakai `showAllOverlays()`/`hideAllOverlays()` yang sudah ada dan dipakai notifikasi), berdasarkan status visibility overlay yang sebenarnya (`overlayWindows.values.any { it.getConfig().isVisible }`) supaya statusnya akurat walau service baru saja start.

# 23
# 11-09-2026-0806-WIB
- Switch "Overlays + Bubble" di Master Controls (`DashboardScreen.kt`) sekarang juga longgar seperti perubahan "All Overlays" di changelog #22: nyala (ON) kalau ADA overlay yang aktif minimal 1 DAN floating bubble aktif — sebelumnya harus SEMUA overlay aktif + bubble aktif baru nyala. Diubah dari `everythingOn = allOverlaysStrictlyOn && bubbleOn` jadi `everythingOn = anyOverlayOn && bubbleOn`, jadi sekarang kedua switch ("All Overlays" & "Overlays + Bubble") sama-sama pakai basis "ada yang aktif", bukan "semua aktif".
- Perilaku nge-tap switch-nya tidak berubah: tetap start/stop SEMUA overlay + bubble sekaligus lewat `onToggleEverything`, cuma indikator ON/OFF-nya yang sekarang lebih longgar.

# 22
# 11-09-2026-0801-WIB
- Switch "All Overlays" di Master Controls (`DashboardScreen.kt`) sekarang nyala (ON) kalau ADA overlay yang aktif minimal 1, bukan cuma kalau SEMUA overlay aktif seperti sebelumnya. Jadi begitu user nyalain 1 overlay lewat toggle satuan di list "Overlays" (atau lewat combo dengan changelog #21 yang bikin bubble ikut nyala), switch "All Overlays" ikut kelihatan ON sebagai indikator status — sesuai permintaan "nyala doang, tidak ikut menyalakan semua overlay lain".
- Perilaku nge-tap switch-nya TIDAK berubah: tetap start/stop SEMUA overlay sekaligus (`onToggleAllOverlays`), cuma tampilan ON/OFF-nya yang sekarang berbasis "ada yang aktif" (`overlays.any { it.isActive }`) alih-alih "semua aktif" (`overlays.all { it.isActive }`).
- Switch "Overlays + Bubble" TIDAK ikut berubah — tetap butuh benar-benar SEMUA overlay aktif + bubble aktif buat nyala (`allOverlaysStrictlyOn && bubbleOn`), supaya switch itu tetap representasi akurat "semuanya benar-benar menyala", bukan ikut jadi longgar seperti switch "All Overlays".

# 21
# 11-09-2026-0756-WIB
- Menyalakan overlay apa pun (lewat toggle satuan di list "Overlays", atau lewat Master Controls "All Overlays"/"Overlays + Bubble") sekarang otomatis ikut menyalakan floating bubble, walau sebelumnya floating bubble dalam kondisi off. Sebelumnya (changelog #20) `startOverlay()` cuma memanggil `createBubble()` KALAU `bubbleEnabled` di memori kebetulan sudah true — jadi kalau user pernah mematikan bubble secara manual, menyalakan overlay baru tidak ikut menyalakan bubble-nya, padahal user butuh bubble untuk kontrol show/hide/lock overlay yang baru aktif itu. Diganti jadi memanggil `setBubbleEnabled(true)` (bukan `createBubble()` langsung) di `OverlayService.startOverlay()`, supaya `AppSettings.bubbleEnabled` ikut ke-persist true juga.
- Karena switch "Floating Bubble" di Master Controls (`DashboardScreen.kt`) sumbernya sama-sama dari `settings.bubbleEnabled` (Flow reaktif dari `OverlayStorage`), begitu `bubbleEnabled` ke-persist true, switch-nya otomatis ikut tampil ON tanpa perlu perubahan tambahan di sisi UI — sudah "sinkron" secara struktural karena satu sumber data yang sama.
- Ini cuma berlaku satu arah: mematikan overlay (satuan maupun semua) TIDAK otomatis mematikan floating bubble — bubble tetap independen untuk arah "off", sesuai desain awal di changelog #20 (biar bubble bisa tetap dipertahankan nyala meski semua overlay dimatikan, kalau memang itu maunya user).

# 20
# 11-09-2026-0744-WIB
- Menambahkan fitur start/stop (BUKAN show/hide visibility) untuk 3 hal: (1) semua overlay sekaligus, (2) floating bubble sendiri, dan (3) overlay+bubble sekaligus. Ditambahkan section baru "Master Controls" di paling atas Dashboard (`DashboardScreen.kt`, komponen baru `MasterControlItem`) berisi 3 baris dengan `AppSwitch`: "All Overlays" (nyala kalau semua overlay yang tersimpan sedang aktif, subtitle "N of M active"), "Floating Bubble" (independen dari overlay), dan "Overlays + Bubble" (gabungan, nyala hanya kalau kedua-duanya nyala). Toggle ini benar-benar create/destroy `OverlayWindow`/`FloatingBubble` lewat `WindowManager` (sama seperti toggle per-overlay yang sudah ada), bukan sekadar `overlay.isVisible`.
- Selama ini floating bubble otomatis ikut lifecycle overlay (`OverlayService.kt`): muncul saat overlay pertama di-start, hilang saat overlay terakhir di-stop — jadi tidak bisa dimatikan/dinyalakan sendiri lepas dari overlay. Diputus ketergantungannya: ditambahkan field baru `AppSettings.bubbleEnabled: Boolean = true` (persisted) dan method baru `OverlayService.setBubbleEnabled(Boolean)` yang independen — bubble sekarang bisa tetap hidup walau semua overlay mati (asal `bubbleEnabled = true`), dan bisa dimatikan walau overlay masih aktif. `createBubble()` dibuat idempoten (`if (floatingBubble != null) return`) supaya aman dipanggil berulang dari beberapa tempat (`onCreate`, `startOverlay`, `setBubbleEnabled`).
- Kondisi `exitService()` otomatis (mematikan foreground service sepenuhnya) di `stopOverlay()` diubah dari "overlay kosong" jadi "overlay kosong DAN bubble juga tidak diaktifkan" (`overlayWindows.isEmpty() && !bubbleEnabled`), supaya service (dan bubble-nya) tidak ikut mati kalau user cuma mematikan semua overlay tapi bubble-nya masih mau tetap nyala. Sebaliknya, `setBubbleEnabled(false)` yang membuat overlay+bubble sama-sama kosong akan menutup service sepenuhnya (`exitService(persistInactive = false)`), supaya tidak ada foreground service nganggur.
- `MainActivity.kt` (`SirohaApp`) ditambah 3 handler baru mengikuti pola `onToggleOverlay` yang sudah ada (cek izin `Settings.canDrawOverlays`, start+bind service kalau belum jalan, lalu panggil service): `onToggleAllOverlays` (loop semua overlay tersimpan, start/stop satu-satu + persist `isActive`), `onToggleBubble` (panggil `setBubbleEnabled`), `onToggleEverything` (gabungan keduanya). Diteruskan sebagai parameter baru ke `DashboardScreen`.

# 19
# 10-09-2026-2035-WIB
- Menambahkan ikon leading Material di setiap item Settings (`SettingsScreen.kt`) yang sebelumnya belum punya ikon sama sekali (cuma "View Logs" dan "Exit App" yang punya). Total 18 item sekarang punya ikon: Default Size (AspectRatio), Default Opacity & Bubble Opacity (Opacity), Remember Position (PinDrop), Overlay Animation (Animation), JavaScript (Code), Media Playback (PlayCircleOutline), Cache & WebView Cache (Storage), Cookies (Cookie), Bubble Size (RadioButtonChecked), Auto Hide (VisibilityOff), Snap to Edge (BorderOuter), Notification Controls (Notifications), Hardware Acceleration (Speed), Reduce Animation (MotionPhotosOff), dan Debug Logging (BugReport). Ditambahkan composable helper baru `SettingLeadingIcon()` di `SettingsScreen.kt` supaya polanya seragam (ukuran ikon default 24dp, warna `onSurfaceVariant`, jarak 16dp ke teks) mengikuti gaya ikon yang sudah ada sebelumnya di "View Logs"/"Exit App".
- Untuk item yang isinya Slider (Default Size, Default Opacity, Bubble Size, Bubble Opacity), `Column` konten diganti dari `Modifier.fillMaxWidth()` jadi `Modifier.weight(1f)` supaya tidak overflow di dalam `Row` sekarang ada ikon di depannya — sebelumnya `fillMaxWidth()` aman karena `Column` itu satu-satunya isi `Row`.
- Ikon-ikon di atas berasal dari dependency `material-icons-extended` yang sudah ada di `app/build.gradle.kts`, tidak ada dependency baru yang ditambahkan.

# 18
# 10-09-2026-2013-WIB
- Memperbaiki bug "Maximum 5 overlays reached" masih muncul walau `maxOverlays` sudah diubah ke `Int.MAX_VALUE` di changelog #17. Penyebabnya `AppSettings` disimpan sebagai JSON (Gson) di DataStore (`OverlayStorage.getSettings()`) — kalau app sudah pernah dipakai sebelumnya, JSON lama yang tersimpan di storage masih berisi `"maxOverlays":5`, dan nilai itu menang saat `gson.fromJson()` dipanggil, menimpa default baru dari kode. Diperbaiki dengan memaksa `settings.copy(maxOverlays = Int.MAX_VALUE)` setelah hasil deserialize di `OverlayStorage.getSettings()`, jadi nilai lama apa pun yang tersimpan di storage user (dari versi sebelum limit dihapus) tidak lagi berpengaruh, tanpa perlu clear data aplikasi.

# 17
# 10-09-2026-2004-WIB
- Menghapus batasan maksimal 5 overlay. Diubah `maxOverlays: Int = 5` menjadi `maxOverlays: Int = Int.MAX_VALUE` di `AppSettings.kt`, sehingga toast "Maximum overlays reached" di `DashboardScreen.kt` (kondisi `overlays.size >= settings.maxOverlays`) praktis tidak akan pernah muncul lagi. Logika pengecekan di `DashboardScreen.kt` tidak diubah, hanya nilai konstanta batasnya. Test `ModelTest.kt` (`assertEquals(5, settings.maxOverlays)`) disesuaikan jadi `assertEquals(Int.MAX_VALUE, settings.maxOverlays)` supaya tidak gagal build.

# 16
# 10-09-2026-1801-WIB
- Memperbesar tombol FAB "Add Image Overlay" (ikon gambar) dan "Add Web Overlay" (ikon globe) di Dashboard (`DashboardScreen.kt`) yang sebelumnya kekecilan. Diganti dari `SmallFloatingActionButton` (40dp) jadi `FloatingActionButton` ukuran normal (56dp), sama seperti tombol FAB utama "Add Overlay".
- Menghapus section "Quick Start Presets" (Donation Alert, Media Share, Leaderboard, Camera Frame, Logo, Custom Web Overlay) di dialog "Add Overlay" (`AddEditOverlayDialog.kt`) karena tidak berguna — tiap preset cuma ngisi field "Overlay Name" doang tanpa isi URL atau konfigurasi lain. Dialog sekarang langsung tampil field "Overlay Name" dan "Overlay URL", tombol "Test URL", serta "Cancel"/"Save" seperti biasa (tidak ada yang dihapus dari bagian ini). Validasi "Overlay Name" wajib diisi sebelum bisa Save tetap ada seperti sebelumnya (`nameError` kalau kosong), cuma sekarang labelnya ditambah tanda "*" (Overlay Name *) biar kelihatan wajib dari tampilan. Model `OverlayPreset.kt` dan test-nya tidak dihapus, cuma sudah tidak dipakai lagi di UI ini.

# 15
# 10-09-2026-1754-WIB
- Ditemukan penyebab SEBENARNYA kenapa Delete overlay di changelog #14 masih belum beres: kalau overlay yang dihapus adalah overlay aktif TERAKHIR, `stopOverlay()` juga memanggil `exitService()` (karena `overlayWindows` jadi kosong). `exitService()` punya tulisan storage-nya sendiri yang UNCONDITIONAL: baca semua overlay dari storage lalu `saveOverlays(overlays.map { it.copy(isActive = false) })` — ini menimpa balik SELURUH list overlay pakai data lama (dibaca sebelum `storage.removeOverlay()` sempat jalan), jadi overlay yang baru saja dihapus ikut ketulis ulang dan "hidup lagi". Ini kejadian terus-menerus (bukan sesekali doang) makanya walau dihapus berkali-kali atau app ditutup-buka tetap muncul lagi — datanya di storage memang balik kebentuk lama tiap kali proses delete jalan.
- Diperbaiki dengan menambah parameter `persistInactive: Boolean = true` juga di `OverlayService.exitService()`, dan `stopOverlay()` sekarang meneruskan nilai `persistInactive` yang sama ke `exitService()` saat dipanggil (`exitService(persistInactive = persistInactive)`). Jadi kalau dipanggil dari alur delete (`persistInactive = false`), `exitService()` tidak menulis apa pun ke storage, dan tidak ada lagi yang bisa menimpa balik `storage.removeOverlay()`.
- Menambahkan logging lebih verbose di sepanjang alur start/stop/delete overlay biar gampang di-trace lewat "View Logs" kalau ada bug serupa lagi: `OverlayService.startOverlay()`, `stopOverlay()`, `exitService()`, `clearAndExit()`, `onDestroy()` sekarang masing-masing log kondisi pentingnya (id, jumlah window aktif, apakah storage ditulis atau di-skip). `OverlayStorage` (`saveOverlays`, `addOverlay`, `updateOverlay`, `removeOverlay`, `saveAppLayouts`, `removeAppLayout`) sekarang log jumlah data sebelum/sesudah tiap operasi. `MainActivity.kt` (`onDeleteOverlay`) log mulai/selesainya proses delete beserta jumlah app layout terkait yang ikut dihapus. Semua pakai `Logger.d()` (Debug level) sehingga tetap butuh toggle "Debug Logging" aktif di Settings supaya kecatat di "View Logs" — sama seperti log level Debug lain di aplikasi ini.

# 14
# 10-09-2026-1748-WIB
- Memperbaiki bug tombol Delete overlay di Dashboard yang kadang tidak beneran menghapus overlay-nya — overlay tetap muncul lagi meski sudah dikonfirmasi dihapus (kelihatan makin jelas setelah pindah ke halaman Settings lalu kembali ke Dashboard). Penyebabnya race condition antara dua proses tulis storage yang jalan hampir bersamaan saat delete: `MainActivity.kt` memanggil `OverlayService.stopOverlay(id)` (yang di dalamnya ada `serviceScope.launch` sendiri untuk menyimpan `isActive = false`) lalu langsung disusul `storage.removeOverlay(id)`. Keduanya sama-sama baca list overlay dulu baru simpan balik (bukan operasi atomik), jadi kalau tulisan dari `stopOverlay` (yang masih membawa data overlay yang mau dihapus) menang telat setelah tulisan `removeOverlay`, overlay yang tadinya sudah dihapus jadi ketulis ulang alias "hidup lagi".
- Diperbaiki dengan menambah parameter `persistInactive: Boolean = true` di `OverlayService.stopOverlay()` — kalau `false`, method ini cuma menutup window overlay-nya secara visual tanpa menulis apa pun ke storage. `MainActivity.kt` sekarang memanggil `stopOverlay(overlay.id, persistInactive = false)` khusus di alur delete, supaya tidak ada tulisan storage yang bisa balapan dengan `storage.removeOverlay()`. Alur toggle on/off biasa (bukan delete) tidak berubah, tetap pakai `persistInactive = true` (default).

# 13
# 10-09-2026-1740-WIB
- Teks detail (package • ukuran • posisi) di "Configured Apps" (`AppLayoutsScreen.kt`) masih kepotong ellipsis walau ruang sudah dilonggarkan di changelog #12, karena package name-nya sendiri memang panjang (mis. "com.foxdebug.acode"). Diganti `maxLines` dari 1 jadi 2 supaya teksnya wrap ke baris kedua alih-alih dipotong "...", ellipsis cuma muncul kalau setelah 2 baris masih belum cukup.

# 12
# 10-09-2026-1736-WIB
- Memperbaiki teks di "Configured Apps" (`AppLayoutsScreen.kt`) yang masih kepotong pendek (mis. "com.foxdebug.acode...") padahal sudah dikasih padding di changelog #11 — penyebabnya bukan padding-nya, tapi ruang untuk teksnya kehabisan karena satu baris berisi ikon app, 2 tombol aksi terpisah (Duplicate & Delete), dan switch sekaligus. Tombol "Duplicate" dan "Delete" digabung jadi satu menu overflow (ikon titik tiga `MoreVert` + `DropdownMenu`), sehingga ruang untuk teks nama app & detail (package • ukuran • posisi) jadi jauh lebih lega. Padding akhir Column teks disesuaikan dari 16dp ke 8dp karena sekarang cuma ada 1 ikon di sisi kanan, bukan 2.

# 11
# 10-09-2026-1733-WIB
- Memperbaiki tampilan item di "Configured Apps" (`AppLayoutsScreen.kt`, halaman App Layouts) yang teks baris kedua (package • ukuran • posisi) kepotong/mepet ke switch di sisi kanan. Ditambahkan `padding(end = 16.dp)` pada `Column` judul+deskripsi, sama seperti perbaikan serupa di `SettingsScreen.kt` (changelog #9).
- Menambahkan ikon aplikasi di setiap item "Configured Apps" yang sebelumnya kosong tanpa ikon sama sekali (beda dengan "Choose App" picker di dialog Add/Edit App Layout yang sudah pakai ikon). Dibuat composable baru `AppLayoutIcon` yang mengambil ikon lewat `InstalledApps.loadIcon()` berdasarkan `packageName`, di-cache dengan `remember(packageName)` supaya tidak query ulang PackageManager tiap recomposition, dengan fallback ikon `Icons.Default.Apps` kalau package tidak ditemukan (mis. sudah di-uninstall).

# 10
# 10-09-2026-1725-WIB
- Menambahkan fitur pemilih aplikasi terpasang di dialog "Add/Edit App Layout" (`AppLayoutDialog.kt`), supaya user tinggal pilih aplikasi dari daftar alih-alih mengetik "App Name" dan "Package Name" manual. Field "App Name"/"Package Name" lama diganti jadi satu baris "App" yang bisa ditekan untuk membuka dialog "Choose App" berisi daftar aplikasi yang ada launcher-nya di HP (ikon + nama + package), lengkap dengan kolom pencarian. Masih ada tombol "App not in the list? Enter it manually" untuk kasus package yang tidak muncul di daftar (mis. sudah dihapus tapi masih ingin dipakai sebagai referensi).
- Menambahkan util baru `util/InstalledApps.kt` yang query `PackageManager.queryIntentActivities(ACTION_MAIN/CATEGORY_LAUNCHER)` di background thread (`Dispatchers.IO`) untuk mengambil daftar aplikasi launchable beserta ikonnya, dan fallback `getApplicationIcon()` untuk package yang sudah tersimpan tapi tidak ada di daftar launcher.
- Menambahkan elemen `<queries>` (intent MAIN/LAUNCHER) di `AndroidManifest.xml`. Ini wajib di Android 11+ (API 30) karena package visibility filtering membatasi hasil `queryIntentActivities`/`getInstalledApplications` kalau tidak dideklarasikan — alternatif yang lebih aman daripada minta permission `QUERY_ALL_PACKAGES` yang dibatasi ketat oleh kebijakan Play Store.

# 9
# 10-09-2026-1712-WIB
- Memperbaiki teks deskripsi di setiap item Settings (ditambahkan di changelog #8) yang jaraknya terlalu mepet ke switch di sisi kanan. Ditambahkan `padding(end = 16.dp)` pada `Column` judul+deskripsi di semua item toggle di `SettingsScreen.kt` supaya ada jarak aman sebelum switch/ikon panah.
- Memperbaiki posisi scroll di layar Settings yang selalu reset ke paling atas setiap kembali dari layar "View Logs". Penyebabnya `rememberScrollState()` dipanggil di dalam `SettingsScreen`, sedangkan navigasi antar layar di `MainActivity.kt` (`SirohaApp`) memakai `when (currentScreen)` yang membuang composable `SettingsScreen` sepenuhnya dari komposisi saat pindah ke `Screen.LOGS` — jadi scroll state lama ikut hilang dan dibuat baru (posisi 0) saat kembali. Diperbaiki dengan meng-hoist `ScrollState` (`settingsScrollState`) ke `SirohaApp` di `MainActivity.kt` yang tetap hidup selama app berjalan, lalu dioper sebagai parameter baru `scrollState` ke `SettingsScreen`.

# 8
# 10-09-2026-1700-WIB
- Menambahkan deskripsi singkat di bawah setiap judul item pada halaman Settings (`SettingsScreen.kt`), mencakup semua section: Overlay Defaults, WebView, Floating Bubble, Notification, Performance, Debug, dan App. Sebelumnya tiap item cuma menampilkan judul singkat (mis. "JavaScript", "Hardware Acceleration") tanpa penjelasan fungsinya. Item yang berupa toggle/slider diubah dari `Text` langsung jadi `Column` berisi judul + deskripsi (`bodySmall`, warna `onSurfaceVariant`), teks judul & switch-nya tetap sejajar seperti sebelumnya.

# 7
# 10-09-2026-1646-WIB
- Menambahkan fitur log viewer di dalam aplikasi. Sebelumnya toggle "Debug Logging" di Settings memang mengaktifkan logging, tapi log-nya cuma tertulis ke Logcat (`adb logcat`) — tidak ada tempat untuk melihatnya dari dalam app. Sekarang `Logger.kt` juga menyimpan log ke buffer in-memory (maks 500 entri, hanya terisi saat Debug Logging aktif), dan ditambahkan layar baru "Debug Logs" (`LogViewerScreen.kt`) yang bisa diakses lewat item "View Logs" di Settings > Debug. Ada tombol copy log ke clipboard dan clear log.

# 6
# 10-09-2026-1637-WIB
- Memperbaiki bug "Default Width" dan "Default Height" di Settings > Overlay Defaults yang tidak bisa diubah. Penyebabnya kedua field itu cuma dirender sebagai `Text` biasa (tampilan doang), belum ada input yang bisa diedit. Sekarang diganti jadi dua `OutlinedTextField` angka (Width & Height) yang langsung tersimpan ke settings saat diketik.

# 5
# 10-09-2026-1618-WIB
- Memperbaiki bug list Overlay tidak langsung muncul setelah menambah overlay baru (baru muncul setelah buka Settings lalu kembali, atau setelah aplikasi ditutup total lalu dibuka lagi). Penyebabnya `storage.getOverlays()`, `storage.getAppLayouts()`, dan `storage.getSettings()` dipanggil langsung di `collectAsState()` sehingga membuat instance Flow baru tiap recomposition, memicu collector di-cancel & restart terus-menerus (race condition) dan kadang melewatkan update terbaru. Diperbaiki dengan menstabilkan instance Flow-nya pakai `remember` di `MainActivity.kt` (`SirohaApp`).

# 4
# 10-09-2026-1612-WIB
- Menambahkan fitur dialog konfirmasi "Keluar Aplikasi" (Exit App) yang muncul saat menekan tombol back di layar Dashboard, dan menambahkan item "Exit App" di menu Settings yang memicu dialog yang sama.
- Memperbaiki bug tombol back: sebelumnya menekan back di layar Settings atau App Layouts langsung menutup/keluar dari aplikasi (finish Activity) alih-alih kembali ke Dashboard/beranda. Sekarang back di kedua layar tersebut mengarahkan kembali ke Dashboard.
- Mengganti aksi keluar dari `finish()` menjadi `moveTaskToBack(true)` supaya floating overlay/service yang masih aktif tetap berjalan di background saat user keluar dari aplikasi.


# Format Changelog
# Contoh
# Nomer-Tanggal-Bulan-Tahun-Jam-WIB
# 3
# 12-09-2026-1645-WIB
- Memperbaiki tampilan Dashboard.
# 2
# 11-09-2026-1630-WIB
- Memperbaiki tampilan Dashboard.
# 1
# 10-09-2026-1600-WIB
- Memperbaiki tampilan Dashboard.