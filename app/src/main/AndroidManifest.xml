<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required on Android 11+ (API 30) package visibility so we can list
         launchable apps for the "Add App Layout" picker via
         PackageManager.queryIntentActivities(ACTION_MAIN/CATEGORY_LAUNCHER).
         This is the scoped alternative to the QUERY_ALL_PACKAGES permission,
         which Play Store restricts to a narrow set of use cases. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- "App Layouts" (posisi/ukuran/opacity overlay per aplikasi) butuh ini
         buat mendeteksi aplikasi yang sedang di depan lewat UsageStatsManager
         (lihat ForegroundAppWatcher.kt). Ini permission "special" yang TIDAK
         bisa diberikan lewat dialog izin biasa: user wajib mengaktifkannya
         manual di Settings > Apps > Special app access > Usage access, makanya
         dideklarasikan di sini tapi tools:ignore="ProtectedPermissions" (lint
         normalnya melarang app pihak-ketiga mendeklarasikan permission sistem
         seperti ini secara langsung). Tanpa izin ini fitur App Layout tetap
         aman: overlay cuma tidak akan pernah otomatis pindah layout. -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".SirohaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Siroha"
        android:hardwareAccelerated="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:targetApi="35">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.OverlayService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Floating overlay display for user-configured web content and images shown above other apps" />
        </service>

    </application>

</manifest>
